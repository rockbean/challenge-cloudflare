{
  "name": "challenge-cloudflare",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Pages + Pages Functions + Turnstile challenge demo",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "dev:local": "wrangler pages dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler pages deploy ./public"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260611.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.8",
    "wrangler": "^4.99.0"
  }
}
